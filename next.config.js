/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["https://i.ibb.co", "avatars.githubusercontent.com"],
  }
}

module.exports = nextConfig
